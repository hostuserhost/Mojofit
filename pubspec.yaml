name: google_ml_kit_example
description: Sample google_ml_kit pose detection plugin.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_ml_kit:
    path: ../
  camera: ^0.10.5+5
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
